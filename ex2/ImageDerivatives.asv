function [Ix, Iy, It] = ImageDerivatives(X, Y)
%IMAGEDERIVATIVES Summary of this function goes here
%   Detailed explanation goes here
    
    Ky = 0.25 * [-1, -1; ...
                  1,  1];
    Kx = -Ky';
    Kt = 0.25 * ones(2, 2);
    CONV_PARAM = 'same';
    
    Ix = conv2(X, Kx, CONV_PARAM) ...
        + conv2(Y, Kx, CONV_PARAM);
    Iy = 
    
end

